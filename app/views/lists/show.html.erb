<h1><%= @list.name %></h1>

<div class="container">
  <div class="cards">
    <% @list.bookmarks.each do |bookmark| %>
      <img src= <%= bookmark.movie.poster_url %> class="card-img-top" alt="Movie image">
          <h5 class="card-title"><%= bookmark.movie.title %></h5>
          <h5 class="card-title"><%= bookmark.movie.rating %></h5>
          <p class="card-text"><%= bookmark.movie.overview %></p>
          <p class="card-text"><%= bookmark.comment%></p>
          <%= link_to "delete this bookmark", bookmark_path(bookmark), method: :delete, class: "btn btn-cancel", data: { confirm: "Are you sure you want to delete this list?" } %>
    <% end %>
  </div>

</div>
  <%= link_to "Add a new Bookmark", new_list_bookmark_path(@list)%>
  <%= link_to "Back to lists", lists_path %>
