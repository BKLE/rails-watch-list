<div>
  <h1>Create a new list!</h1>
</div>

<div class="conatiner">
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name, placeholder: 'Name your list here!' %>
    <%= f.submit "Create", class: "btn btn-primary"  %>
  <% end %>
</div>
